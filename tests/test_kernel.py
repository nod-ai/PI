# def test_kernel(a: float, b: float, c: float = 1.0, d=2.0) -> (float, int):
# def test_kernel(a: float, b: float, c: float = 1.0, d=2.0) -> (float, int):
# def test_kernel(a: float, b: float, c: float = 1.0, d=2.0) -> (float, int):
# def test_kernel(a: float, b: float, c: float = 1.0, d=2.0) -> (float, int):
def test_kernel(a: float, b: float):
    for i in range(10):
        k = a * b
    return k
